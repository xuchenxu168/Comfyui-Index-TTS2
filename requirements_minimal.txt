# ================================================================
# ComfyUI IndexTTS2 Plugin - Minimal Core Dependencies
# ================================================================
# 最小核心依赖列表 - 仅包含必需的包
# Minimal core dependencies - only essential packages
# 
# 安装方法 / Installation:
# pip install -r requirements_minimal.txt
# ================================================================

# ================================================================
# CORE PYTORCH & DEEP LEARNING (必需 / Required)
# ================================================================
torch>=2.1.2
torchaudio>=2.1.2
numpy>=1.26.2

# ================================================================
# TRANSFORMERS & NLP (必需 / Required)
# ================================================================
transformers>=4.36.2
tokenizers>=0.15.0
accelerate>=0.20.0
sentencepiece>=0.1.99
huggingface-hub>=0.17.0

# ================================================================
# AUDIO PROCESSING (必需 / Required)
# ================================================================
librosa>=0.10.1
soundfile>=0.12.1

# ================================================================
# SCIENTIFIC COMPUTING (必需 / Required)
# ================================================================
scipy>=1.11.0
omegaconf>=2.3.0
einops>=0.8.1
safetensors>=0.4.0

# ================================================================
# TEXT PROCESSING (必需 / Required)
# ================================================================
jieba>=0.42.1
cn2an>=0.5.22
g2p-en>=2.1.0

# ================================================================
# BASIC UTILITIES (必需 / Required)
# ================================================================
requests>=2.31.0
tqdm>=4.65.0

# ================================================================
# PLATFORM-SPECIFIC TEXT PROCESSING (必需 / Required)
# ================================================================
WeTextProcessing>=1.0.3; platform_system != "Darwin"
wetext>=1.0.0; platform_system == "Darwin"
